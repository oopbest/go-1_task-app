global:
  scrape_interval: 5s # ให้ Prometheus ไปดึงข้อมูลทุก 5 วินาที
  evaluation_interval: 5s

scrape_configs:
  - job_name: "task-api"
    static_configs:
      - targets: ["host.docker.internal:8080"] # ดึง Metrics จาก Task API ที่รันบนเครื่องเรา
